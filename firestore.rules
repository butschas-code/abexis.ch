rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * Public read: published blog posts (anonymous Web SDK / SSR fallback without Admin SDK).
     * Writes remain authenticated only.
     */
    match /posts/{postId} {
      allow read: if resource.data.status == "published";
      allow write: if request.auth != null;
    }

    match /vacancies/{vacancyId} {
      allow read: if resource.data.status == "published";
      allow write: if request.auth != null;
    }

    /** Category labels for filters / UI : no secrets; public read. */
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    /**
     * CMS TOTP secrets (custom MFA, free-tier Firebase Auth).
     * Only the Firebase Admin SDK may read/write — clients never access these docs.
     */
    match /cms_totp/{uid} {
      allow read, write: if false;
    }

    match /cms_totp_pending/{uid} {
      allow read, write: if false;
    }

    /** Everything else (authors, users, submissions, media, settings, …). */
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
